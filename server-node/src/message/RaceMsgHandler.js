/** Port của avatar/message/RaceMsgHandler.java */
import { MessageHandler } from './MessageHandler.js';

export class RaceMsgHandler extends MessageHandler {
  constructor(client) {
    super(client);
  }
}

export default RaceMsgHandler;
